SELECT DISTINCT N.COD_EMPL,N.COD_CCOS , C.NOM_CCOS
FROM nm_preno N ,GN_CCOST C
WHERE tip_nomi <> 'D' AND
N.COD_CCOS=C.COD_CCOS AND
(COD_EMPL NOT IN(SELECT COD_EMPL FROM NM_preno WHERE COD_CONC in(2001,2002,2005,2006) ) ) ORDER BY N.COD_CCOS
